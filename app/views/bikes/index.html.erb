    <div class="row justify-content-center">
        <% @bikes.each do |bike| %>
        <div class="col-lg-4 col-12  p-2">

            <div class="card" style="height: 100%;">
                <div class="card-body">
                <div class="card-title text-center">
                    <p><strong> <%= link_to bike.title, bike_path(bike)%></strong></p>
                </div>
                <div class="card-text"><%=bike.description%></div>
            </div>
            </div>
        </div>
                <% end %>

        </div>
